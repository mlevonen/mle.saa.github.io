import { fetchWindGridSeries } from "../api/openMeteoWind.js"; import { renderWindFlow } from "../charts/windFlow.js"; import { drawMapBackground } from "../charts/miniMapBackground.js"; export async function renderWindFlowAnimation(containerEl, lat, lon) { let localStop = null; function stop() { if (localStop) { localStop(); localStop = null; } } const flowCanvas = containerEl.querySelector(".wind-flow-canvas"); const flowBgCanvas = containerEl.querySelector(".wind-flow-bg"); const flowButtons = containerEl.querySelectorAll(".wind-flow-btn"); const flowSlider = containerEl.querySelector(".wind-flow-slider"); const flowTimeLabel = containerEl.querySelector(".wind-flow-time-label"); const flowTicksEl = containerEl.querySelector(".wind-flow-ticks"); const flowSpeedLabel = containerEl.querySelector(".wind-flow-speed-label"); if (!flowCanvas) return { stop, data: null };                 function syncCanvasResolution(canvas) { if (!canvas) return; const rect = canvas.getBoundingClientRect(); const w = Math.max(1, Math.round(rect.width)); const h = Math.max(1, Math.round(rect.height)); if (canvas.width !== w) canvas.width = w; if (canvas.height !== h) canvas.height = h; } syncCanvasResolution(flowCanvas); syncCanvasResolution(flowBgCanvas); let windSeriesData = null; function renderFlowTicks(maxIdx) { if (!flowTicksEl) return; const tickCount = maxIdx >= 4 ? 5 : maxIdx + 1; const ticks = []; for (let i = 0; i < tickCount; i++) { ticks.push(Math.round((maxIdx * i) / (tickCount - 1))); } flowTicksEl.innerHTML = ticks .map(h => `<span>${h === 0 ? "nyt" : h + "h"}</span>`) .join(""); } function showWindFlowOffset(offsetHours) { if (!windSeriesData || !windSeriesData.series.length) return; const idx = Math.max( 0, Math.min(windSeriesData.series.length - 1, offsetHours) ); stop(); localStop = renderWindFlow(flowCanvas, { grid: windSeriesData.series[idx], size: windSeriesData.size }); flowButtons.forEach(btn => { btn.classList.toggle("active", Number(btn.dataset.offset) === idx); }); if (flowSlider) flowSlider.value = idx; if (flowTimeLabel) { const hourDate = windSeriesData.hours[idx]; flowTimeLabel.textContent = idx === 0 ? "Nyt" : `+${idx}h (${hourDate.toLocaleTimeString("fi-FI", { hour: "2-digit", minute: "2-digit" })})`; } if (flowSpeedLabel) { const wind = windSeriesData.stationWind?.[idx]; if (wind && wind.speed != null) { const speedTxt = wind.speed.toFixed(1); const gustTxt = wind.gust != null ? `, puuskat ${wind.gust.toFixed(1)} m/s` : ""; flowSpeedLabel.textContent = `💨 ${speedTxt} m/s${gustTxt}`; } else { flowSpeedLabel.textContent = ""; } } } flowButtons.forEach(btn => { btn.onclick = () => showWindFlowOffset(Number(btn.dataset.offset)); }); if (flowSlider) { flowSlider.oninput = () => showWindFlowOffset(Number(flowSlider.value)); } try {                   const aspectRatio = flowCanvas.height > 0 ? flowCanvas.width / flowCanvas.height : 1; windSeriesData = await fetchWindGridSeries(lat, lon, aspectRatio); if (flowBgCanvas) { drawMapBackground(flowBgCanvas, windSeriesData.bounds).catch(err => { console.warn("Karttataustan lataus epäonnistui:", err); }); } const maxIdx = windSeriesData.series.length - 1; if (flowSlider) { flowSlider.max = maxIdx; } renderFlowTicks(maxIdx);     const lastBtn = containerEl.querySelector(".wind-flow-btn-last"); if (lastBtn) { lastBtn.dataset.offset = maxIdx; lastBtn.textContent = `💨 ${maxIdx}h (viimeisin)`; } showWindFlowOffset(0); } catch (err) { console.warn("Tuulivirtauksen haku epäonnistui:", err); } return { stop, data: windSeriesData }; }
