import { getLatestObservation } from "../utils/helpers.js"; export function updatePopupTitles(popupEl, data) { const wind = getLatestObservation(data.obsWindSpeed, "utctime", "windspeedms"); const gust = getLatestObservation(data.obsWindSpeed, "utctime", "windgust"); const pressure = getLatestObservation(data.obsPressure, "utctime", "pressure");       if (wind) {               const windObsTitle = popupEl.querySelector(".wind-obs-card-title");       const windObsStrong = windObsTitle?.querySelector("strong") || windObsTitle; if (windObsStrong) { windObsStrong.textContent = `Tuuli ${wind.v.toFixed(1)} m/s` + (gust ? ` (puuskat ${gust.v.toFixed(1)} m/s)` : ""); } }}
