import { fetchSeaLevel, findNearestSeaLevelStation } from "../api/sealevel.js"; export async function renderSeaLevelCard(containerEl, station) { const seaLevelCard = containerEl.querySelector(".popup-sealevel-card"); const seaLevelWatlevEl = containerEl.querySelector('.wind-flow-sealevel-value[data-kind="watlev"]'); const seaLevelN2000El = containerEl.querySelector('.wind-flow-sealevel-value[data-kind="n2000"]'); if (station.inland) { if (seaLevelCard) seaLevelCard.style.display = "none"; return; } if (seaLevelCard) seaLevelCard.style.display = ""; if (!seaLevelWatlevEl && !seaLevelN2000El) return; const nearestSea = findNearestSeaLevelStation(station.lat, station.lon); const formatLevel = v => v != null ? `${v > 0 ? "+" : ""}${v} cm` : "–"; if (!nearestSea) { if (seaLevelWatlevEl) seaLevelWatlevEl.textContent = "–"; if (seaLevelN2000El) seaLevelN2000El.textContent = "–"; return; }             try { const { watlev, n2000 } = await fetchSeaLevel(nearestSea.fmisid); if (seaLevelWatlevEl) { seaLevelWatlevEl.textContent = `${formatLevel(watlev)} (${nearestSea.name})`; seaLevelWatlevEl.title = nearestSea.name; } if (seaLevelN2000El) { seaLevelN2000El.textContent = `${formatLevel(n2000)} (${nearestSea.name})`; seaLevelN2000El.title = nearestSea.name; } } catch (err) { if (seaLevelWatlevEl) { seaLevelWatlevEl.textContent = `– (${nearestSea.name})`; seaLevelWatlevEl.title = nearestSea.name; } if (seaLevelN2000El) { seaLevelN2000El.textContent = `– (${nearestSea.name})`; seaLevelN2000El.title = nearestSea.name; } } }
