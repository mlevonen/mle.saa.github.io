import { fetchSeaLevel, findNearestSeaLevelStation } from "../api/sealevel.js"; export function waterTempColor(t) { if (t == null) return "#9e9e9e"; return t < 10 ? "#0277bd" : t < 15 ? "#0097a7" : t < 18 ? "#00897b" : t < 22 ? "#43a047" : "#fb8c00"; } export async function renderSeaLevelCard(containerEl, station) { const seaLevelCard = containerEl.querySelector(".popup-sealevel-card"); const seaLevelWatlevEl = containerEl.querySelector('.wind-flow-sealevel-value[data-kind="watlev"]'); const seaLevelN2000El = containerEl.querySelector('.wind-flow-sealevel-value[data-kind="n2000"]'); const waterTempWrapper = containerEl.querySelector(".current-watertemp-block"); const waterTempValueEl = containerEl.querySelector(".current-watertemp-value"); const waterTempCircleEl = containerEl.querySelector(".current-watertemp-circle"); const waterTempSourceEl = containerEl.querySelector(".current-watertemp-source"); if (station.inland) { if (seaLevelCard) seaLevelCard.style.display = "none"; if (waterTempWrapper) waterTempWrapper.style.display = "none"; return; } if (seaLevelCard) seaLevelCard.style.display = ""; if (!seaLevelWatlevEl && !seaLevelN2000El && !waterTempWrapper) return; const nearestSea = findNearestSeaLevelStation(station.lat, station.lon); const formatLevel = v => v != null ? `${v > 0 ? "+" : ""}${v} cm` : "–"; if (!nearestSea) { if (seaLevelWatlevEl) seaLevelWatlevEl.textContent = "–"; if (seaLevelN2000El) seaLevelN2000El.textContent = "–"; if (waterTempWrapper) waterTempWrapper.style.display = "none"; return; } try { const { watlev, n2000, waterTemp } = await fetchSeaLevel(nearestSea.fmisid); if (seaLevelWatlevEl) { seaLevelWatlevEl.textContent = `${formatLevel(watlev)} (${nearestSea.name})`; seaLevelWatlevEl.title = nearestSea.name; } if (seaLevelN2000El) { seaLevelN2000El.textContent = `${formatLevel(n2000)} (${nearestSea.name})`; seaLevelN2000El.title = nearestSea.name; } if (waterTempWrapper) { if (Number.isFinite(waterTemp)) { if (waterTempValueEl) waterTempValueEl.textContent = `${waterTemp.toFixed(1)} °C`; if (waterTempCircleEl) waterTempCircleEl.style.backgroundColor = waterTempColor(waterTemp); if (waterTempSourceEl) { waterTempSourceEl.textContent = `Lähin: ${nearestSea.name}`; waterTempSourceEl.title = nearestSea.name; } waterTempWrapper.style.display = ""; } else { waterTempWrapper.style.display = "none"; } } } catch (err) { if (seaLevelWatlevEl) { seaLevelWatlevEl.textContent = `– (${nearestSea.name})`; seaLevelWatlevEl.title = nearestSea.name; } if (seaLevelN2000El) { seaLevelN2000El.textContent = `– (${nearestSea.name})`; seaLevelN2000El.title = nearestSea.name; } if (waterTempWrapper) waterTempWrapper.style.display = "none"; } }
