import { groupBySeaArea } from "./seaAreas.js"; import { mobileStationDetailHTML, renderMobileStationDetail } from "./popup/mobileStationDetail.js"; const listEl = document.getElementById("station-list"); const overlayEl = document.getElementById("detail-overlay"); const sheetEl = document.getElementById("detail-sheet"); const sheetBodyEl = document.getElementById("detail-sheet-body"); const closeBtn = document.getElementById("detail-close");     let currentStop = null; function closeSheet() { if (currentStop) { currentStop(); currentStop = null; } overlayEl.classList.remove("open"); sheetBodyEl.innerHTML = ""; } closeBtn.addEventListener("click", closeSheet);   overlayEl.addEventListener("click", e => { if (e.target === overlayEl) closeSheet(); }); document.addEventListener("keydown", e => { if (e.key === "Escape" && overlayEl.classList.contains("open")) closeSheet(); }); async function openStation(station) {     if (currentStop) { currentStop(); currentStop = null; } sheetBodyEl.innerHTML = mobileStationDetailHTML(station); overlayEl.classList.add("open");             sheetEl.scrollTop = 0; const { stop } = await renderMobileStationDetail(sheetBodyEl, station); currentStop = stop; }                 const coastalStations = stations.filter(s => s.type === "coastal"); const groups = groupBySeaArea(coastalStations); if (!groups.length) { const empty = document.createElement("p"); empty.className = "empty-note"; empty.textContent = "Havaintopisteitä ei löytynyt."; listEl.appendChild(empty); } else { groups.forEach(({ area, stations: areaStations }) => { const section = document.createElement("section"); section.className = "sea-area-section"; const heading = document.createElement("h2"); heading.className = "sea-area-title"; heading.textContent = area; section.appendChild(heading); const ul = document.createElement("ul"); ul.className = "station-list"; areaStations .slice() .sort((a, b) => a.name.localeCompare(b.name, "fi")) .forEach(station => { const li = document.createElement("li"); const btn = document.createElement("button"); btn.type = "button"; btn.className = "station-item"; btn.innerHTML = `
          <span class="station-item-name">${station.name}</span>
          <span class="station-item-chevron">›</span>
        `; btn.addEventListener("click", () => openStation(station)); li.appendChild(btn); ul.appendChild(li); }); section.appendChild(ul); listEl.appendChild(section); }); }
