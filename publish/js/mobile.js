import { groupBySeaArea } from "./seaAreas.js"; import { mobileStationDetailHTML, renderMobileStationDetail } from "./popup/mobileStationDetail.js"; import { getFavoriteIds, isFavorite, toggleFavorite } from "./utils/favorites.js"; const listEl = document.getElementById("station-list"); const overlayEl = document.getElementById("detail-overlay"); const sheetEl = document.getElementById("detail-sheet"); const sheetBodyEl = document.getElementById("detail-sheet-body"); const closeBtn = document.getElementById("detail-close");     let currentStop = null; function closeSheet() { if (currentStop) { currentStop(); currentStop = null; } overlayEl.classList.remove("open"); sheetBodyEl.innerHTML = ""; } closeBtn.addEventListener("click", closeSheet);   overlayEl.addEventListener("click", e => { if (e.target === overlayEl) closeSheet(); }); document.addEventListener("keydown", e => { if (e.key === "Escape" && overlayEl.classList.contains("open")) closeSheet(); }); async function openStation(station) {     if (currentStop) { currentStop(); currentStop = null; } sheetBodyEl.innerHTML = mobileStationDetailHTML(station); overlayEl.classList.add("open");             sheetEl.scrollTop = 0; const { stop } = await renderMobileStationDetail(sheetBodyEl, station); currentStop = stop; }                   function updateFavoriteButtons(stationId, isFav) { document.querySelectorAll(`.station-favorite-btn[data-station-id="${stationId}"]`) .forEach(btn => { btn.classList.toggle("is-favorite", isFav); btn.setAttribute("aria-pressed", String(isFav)); btn.setAttribute("aria-label", isFav ? "Poista suosikeista" : "Lisää suosikiksi"); btn.textContent = isFav ? "★" : "☆"; }); } function handleToggleFavorite(stationId) { const isFav = toggleFavorite(stationId); updateFavoriteButtons(stationId, isFav); refreshFavoritesSection(); } function createStationLi(station) { const li = document.createElement("li"); const row = document.createElement("div"); row.className = "station-item"; const fav = isFavorite(station.id); const favBtn = document.createElement("button"); favBtn.type = "button"; favBtn.className = "station-favorite-btn" + (fav ? " is-favorite" : ""); favBtn.dataset.stationId = station.id; favBtn.setAttribute("aria-pressed", String(fav)); favBtn.setAttribute("aria-label", fav ? "Poista suosikeista" : "Lisää suosikiksi"); favBtn.textContent = fav ? "★" : "☆"; favBtn.addEventListener("click", e => {           e.stopPropagation(); handleToggleFavorite(station.id); }); const openBtn = document.createElement("button"); openBtn.type = "button"; openBtn.className = "station-item-open"; openBtn.innerHTML = `
    <span class="station-item-name">${station.name}</span>
    <span class="station-item-chevron">›</span>
  `; openBtn.addEventListener("click", () => openStation(station)); row.appendChild(favBtn); row.appendChild(openBtn); li.appendChild(row); return li; }                 const coastalStations = stations.filter(s => s.type === "coastal"); const groups = groupBySeaArea(coastalStations);         const favoritesSection = document.createElement("section"); favoritesSection.className = "sea-area-section favorites-section"; favoritesSection.style.display = "none"; const favoritesHeading = document.createElement("h2"); favoritesHeading.className = "sea-area-title favorites-title"; favoritesHeading.textContent = "★ Suosikit"; favoritesSection.appendChild(favoritesHeading); const favoritesList = document.createElement("ul"); favoritesList.className = "station-list"; favoritesSection.appendChild(favoritesList); listEl.appendChild(favoritesSection); function refreshFavoritesSection() { const favIds = getFavoriteIds(); const favStations = coastalStations.filter(s => favIds.includes(s.id));     const ordered = groupBySeaArea(favStations) .flatMap(g => g.stations.slice().sort((a, b) => a.name.localeCompare(b.name, "fi"))); favoritesList.innerHTML = ""; ordered.forEach(station => favoritesList.appendChild(createStationLi(station))); favoritesSection.style.display = ordered.length ? "" : "none"; } if (!groups.length) { const empty = document.createElement("p"); empty.className = "empty-note"; empty.textContent = "Havaintopisteitä ei löytynyt."; listEl.appendChild(empty); } else { groups.forEach(({ area, stations: areaStations }) => { const section = document.createElement("section"); section.className = "sea-area-section"; const heading = document.createElement("h2"); heading.className = "sea-area-title"; heading.textContent = area; section.appendChild(heading); const ul = document.createElement("ul"); ul.className = "station-list"; areaStations .slice() .sort((a, b) => a.name.localeCompare(b.name, "fi")) .forEach(station => ul.appendChild(createStationLi(station))); section.appendChild(ul); listEl.appendChild(section); }); } refreshFavoritesSection();
