const INLAND_LAKE_STATION_IDS = new Set([ "asikkala-pulkkilanharju-weather", "luhanka-judinsalo-weather", "tampere-siilinkari-weather", "lappeenranta-hiekkapakka-weather", "rantasalmi-rukkasluoto-weather", "liperi-tuiskavanluoto-weather", "kuopio-ritoniemi-weather", "inari-seitalaassa-weather" ]);   export const SEA_AREA_ORDER = [ "Suomenlahti", "Saaristomeri ja Ahvenanmaa", "Selkämeri", "Merenkurkku", "Perämeri", "Sisämaa" ]; export function getSeaArea(station) { if (INLAND_LAKE_STATION_IDS.has(station.id)) { return "Sisämaa"; } const { lat, lon } = station; if (lat >= 63.8) return "Perämeri"; if (lat >= 62.3 && lon <= 22.6) return "Merenkurkku"; if (lat >= 60.65 && lon <= 22.2) return "Selkämeri"; if (lon <= 23.0) return "Saaristomeri ja Ahvenanmaa"; return "Suomenlahti"; }     export function groupBySeaArea(stationList) { const groups = {}; for (const station of stationList) { const area = getSeaArea(station); if (!groups[area]) groups[area] = []; groups[area].push(station); } return SEA_AREA_ORDER .filter(area => groups[area]?.length) .map(area => ({ area, stations: groups[area] })); }
