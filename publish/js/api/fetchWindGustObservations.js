export async function fetchWindGustObservations(fmisid) { const url = `https://opendata.fmi.fi/wfs?service=WFS&version=2.0.0&request=GetFeature&storedquery_id=fmi::observations::weather::timevaluepair&fmisid=${fmisid}&parameters=wg_10min`; const res = await fetch(url); const text = await res.text(); const parser = new DOMParser(); const xml = parser.parseFromString(text, "text/xml"); const points = xml.getElementsByTagNameNS("*", "MeasurementTVP"); const gusts = []; for (let i = 0; i < points.length; i++) { const timeNode = points[i].getElementsByTagNameNS("*", "time")[0]; const valueNode = points[i].getElementsByTagNameNS("*", "value")[0]; if (!timeNode || !valueNode) continue; const value = Number(valueNode.textContent); if (isNaN(value)) continue; gusts.push({ utctime: timeNode.textContent, windgust: value }); } return gusts; }
