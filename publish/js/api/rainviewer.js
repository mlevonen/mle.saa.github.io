const API_URL = "https://api.rainviewer.com/public/weather-maps.json"; export async function fetchRadarFrames() { const res = await fetch(API_URL); if (!res.ok) { throw new Error("RainViewer-tutkadatan haku epäonnistui"); } const data = await res.json(); const past = data?.radar?.past ?? []; return { host: data.host, frames: past.map(f => ({ time: new Date(f.time * 1000), path: f.path })) }; }     export function radarTileUrl(host, path) { return `${host}${path}/256/{z}/{x}/{y}/2/1_1.png`; }
