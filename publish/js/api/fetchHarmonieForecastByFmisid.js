const DEFAULT_HOURS_AHEAD = 54; export async function fetchHarmonieForecastByFmisid(fmisid, hoursAhead = DEFAULT_HOURS_AHEAD) { const endtime = new Date(Date.now() + hoursAhead * 60 * 60 * 1000).toISOString(); const url = `https://opendata.fmi.fi/wfs?service=WFS&version=2.0.0&request=getFeature&storedquery_id=fmi::forecast::harmonie::surface::point::multipointcoverage&fmisid=${fmisid}&endtime=${endtime}`; const res = await fetch(url); const text = await res.text(); const parser = new DOMParser(); const xml = parser.parseFromString(text, "text/xml");       const positionsNode = xml.getElementsByTagNameNS("*", "positions")[0]; if (!positionsNode) { console.error("No positions found in XML"); return null; } const posValues = positionsNode.textContent.trim().split(/\s+/); const times = []; for (let i = 2; i < posValues.length; i += 3) { times.push(Number(posValues[i]) * 1000); }       const valuesNode = xml.getElementsByTagNameNS("*", "doubleOrNilReasonTupleList")[0]; if (!valuesNode) { console.error("No values block found in XML"); return null; } const values = valuesNode.textContent .trim() .split(/\s+/) .map(v => (v === "NaN" ? null : Number(v))); const blockSize = 21; const fcTemp = []; const fcWindSpeed = []; const fcWindDir = []; const fcWindGust = []; times.forEach((time, i) => { const offset = i * blockSize; fcTemp.push({ utctime: new Date(time).toISOString(), temperature: values[offset + 2] }); fcWindDir.push({ utctime: new Date(time).toISOString(), winddirection: values[offset + 5] }); fcWindSpeed.push({ utctime: new Date(time).toISOString(), windspeedms: values[offset + 6] }); fcWindGust.push({ utctime: new Date(time).toISOString(), windgust: values[offset + 20] }); }); return { fcTemp, fcWindSpeed, fcWindDir, fcWindGust }; }
