import { fetchObservationSeriesByFmisid } from "./dataLoader.js"; import { getSmartSymbol } from "../popup/popupExtras.js"; import { loadPreviewCache, savePreviewCache } from "../utils/previewCache.js"; const CACHE_KEY = "weatherPreviewCache"; const CACHE_TTL = 5 * 60 * 1000; function createWeatherIcon(temp) { const html = `
    <div class="weather-marker">
      <div class="weather-temp">${Math.round(temp)}°</div>
    </div>
  `; return L.divIcon({ className: "weather-marker", html, iconSize: [42, 42] }); } function updateWeatherMarkers(values, markerRegistry) { Object.entries(values).forEach(([fmisid, data]) => { const marker = markerRegistry[fmisid]; if (!marker) return;   if (marker.station?.type !== "weather") return; if ( marker.previewData?.temp === data.temp && marker.previewData?.symbol === data.symbol ) { return; } marker.previewData = marker.previewData || {}; marker.previewData.temp = data.temp; marker.previewData.symbol = data.symbol; const icon = createWeatherIcon(data.temp); marker.setIcon(icon); }); }       export function loadWeatherPreviewCache(markerRegistry) { const cached = loadPreviewCache(CACHE_KEY, CACHE_TTL); if (!cached) return; updateWeatherMarkers(cached, markerRegistry); } export async function updateWeatherPreview( stations, markerRegistry ) { const weatherStations = stations.filter( s => s.type === "weather" && s.featured ); const values = {};     await Promise.all(weatherStations.map(async station => { const series = await fetchObservationSeriesByFmisid( station.fmisid ); if (!series.length) return; const latest = series.at(-1); values[station.fmisid] = { temp: latest.temperature, symbolNow: getSmartSymbol(series) }; })); updateWeatherMarkers( values, markerRegistry ); savePreviewCache(CACHE_KEY, values); }
