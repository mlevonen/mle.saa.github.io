const SYKE_URL = "https://rajapinnat.ymparisto.fi/api/Hydrologiarajapinta/1.2/odata/LampoPintavesi"; const UIRAS_URL = "https://iot.fvh.fi/opendata/uiras/uiras_latest.geojson"; const SYKE_LOOKBACK_DAYS = 4; const DEDUPE_RADIUS_KM = 3; let cache = null; let cacheTime = 0; const CACHE_TTL = 15 * 60 * 1000; function approxDistanceKm(lat1, lon1, lat2, lon2) { const dLat = (lat1 - lat2) * 111.32; const dLon = (lon1 - lon2) * 111.32 * Math.cos((lat1 + lat2) / 2 * Math.PI / 180); return Math.sqrt(dLat * dLat + dLon * dLon); } function dmsToDecimal(value) { const s = String(value).padStart(6, "0"); const deg = Number(s.slice(0, 2)); const min = Number(s.slice(2, 4)); const sec = Number(s.slice(4, 6)); if (!Number.isFinite(deg) || !Number.isFinite(min) || !Number.isFinite(sec)) return null; return deg + min / 60 + sec / 3600; } function isNearExistingStation(lat, lon) { if (typeof stations === "undefined" || !Array.isArray(stations)) return false; return stations.some(s => approxDistanceKm(lat, lon, s.lat, s.lon) < DEDUPE_RADIUS_KM ); } async function fetchSykeWaterTemps() { const since = new Date(Date.now() - SYKE_LOOKBACK_DAYS * 24 * 3600 * 1000); const sinceStr = since.toISOString().slice(0, 19); const params = new URLSearchParams({ "$filter": `Aika gt datetime'${sinceStr}'`, "$expand": "Paikka", "$orderby": "Aika desc" }); try { const res = await fetch(`${SYKE_URL}?${params}`); if (!res.ok) { console.warn("SYKE-vedenlämpöhaku epäonnistui:", res.status); return []; } const json = await res.json(); const rows = Array.isArray(json.value) ? json.value : []; const byPlace = new Map(); for (const row of rows) { const paikkaId = row.Paikka_Id; if (byPlace.has(paikkaId)) continue; const temp = Number(row.Arvo); if (!Number.isFinite(temp)) continue; const paikka = row.Paikka; if (!paikka) continue; const lat = dmsToDecimal(paikka.KoordLat); const lon = dmsToDecimal(paikka.KoordLong); if (lat == null || lon == null) continue; byPlace.set(paikkaId, { id: `syke-${paikkaId}`, name: paikka.Nimi || paikka.JarviNimi || "Sisävesi", lat, lon, temp, time: row.Aika, source: "syke" }); } return Array.from(byPlace.values()); } catch (err) { console.warn("SYKE-vedenlämpöhaku epäonnistui:", err); return []; } } async function fetchHelsinkiAreaWaterTemps() { try { const res = await fetch(UIRAS_URL); if (!res.ok) { console.warn("UiRaS-vedenlämpöhaku epäonnistui:", res.status); return []; } const json = await res.json(); const features = Array.isArray(json.features) ? json.features : []; const result = []; for (const f of features) { const m = f?.properties?.measurement; const temp = Number(m?.temp_water); const coords = f?.geometry?.coordinates; if (!Number.isFinite(temp) || !Array.isArray(coords) || coords.length < 2) continue; result.push({ id: `uiras-${f.id}`, name: f.properties.name || f.properties.location || "Uimapaikka", lat: coords[1], lon: coords[0], temp, time: m.time, source: "uiras" }); } return result; } catch (err) { console.warn("UiRaS-vedenlämpöhaku epäonnistui:", err); return []; } } export async function fetchWaterTempPoints() { if (cache && Date.now() - cacheTime < CACHE_TTL) { return cache; } const [syke, uiras] = await Promise.all([ fetchSykeWaterTemps(), fetchHelsinkiAreaWaterTemps() ]); const all = [...syke, ...uiras].filter(p => !isNearExistingStation(p.lat, p.lon)); cache = all; cacheTime = Date.now(); return all; }
