const STORAGE_KEY = "merisaa_favorites"; function readFavoriteIds() { try { const raw = localStorage.getItem(STORAGE_KEY); const parsed = raw ? JSON.parse(raw) : []; return Array.isArray(parsed) ? parsed : []; } catch {       return []; } } function writeFavoriteIds(ids) { try { localStorage.setItem(STORAGE_KEY, JSON.stringify(ids)); } catch {   } } export function getFavoriteIds() { return readFavoriteIds(); } export function isFavorite(stationId) { return readFavoriteIds().includes(stationId); }   export function toggleFavorite(stationId) { const ids = readFavoriteIds(); const idx = ids.indexOf(stationId); if (idx === -1) { ids.push(stationId); } else { ids.splice(idx, 1); } writeFavoriteIds(ids); return idx === -1; }
