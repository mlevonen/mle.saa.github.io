export async function fetchSmartSymbolsByFmisid(fmisid) { console.log("fetch smartsymbol fmisid:", fmisid); const url = `https://opendata.fmi.fi/wfs?service=WFS&version=2.0.0` + `&request=getFeature` + `&storedquery_id=fmi::forecast::harmonie::surface::point::multipointcoverage` + `&fmisid=${fmisid}` + `&parameters=smartsymbol`; const res = await fetch(url); const text = await res.text(); const xml = new DOMParser().parseFromString(text, "text/xml");   const positionsNode = xml.getElementsByTagNameNS("*", "positions")[0]; const symbolsNode = xml.getElementsByTagNameNS("*", "doubleOrNilReasonTupleList")[0]; if (!positionsNode || !symbolsNode) { console.warn("SmartSymbol XML nodes not found"); return []; } const pos = positionsNode.textContent.trim().split(/\s+/); const sym = symbolsNode.textContent.trim().split(/\s+/).map(Number); const result = []; for (let i = 0; i < sym.length; i++) { const unix = Number(pos[i * 3 + 2]); result.push({ time: new Date(unix * 1000), symbol: sym[i] }); } console.log("SMART SYMBOLS PARSED:", result.slice(0,5)); return result; }
